class Preview : Element
{
    function attached() {
        self.on("selectedColorChanged", function(evt) {
            $(#colorPreview).style#background-color=$(#colorsContainer)[evt.data.index].style#background-color;
        });
    }
}